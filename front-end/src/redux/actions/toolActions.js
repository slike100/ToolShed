import store from '../store';
import { toolBaseUrl } from '../../utils/globalConstants';

import {
    //Import tool types here
} from "../types/toolTypes";

// example action creator:
// export const FUNCTION = (params) => {

// action logic goes here 

//     const action = {
//         type: TypeError,
//         payload: ??
//     }
//     return action (alternatively use store.dispatch(action) if you are using an async action and thunk)
// }